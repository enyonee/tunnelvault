[project]
name = "tunnelvault"
version = "1.2"
requires-python = ">=3.10"
dependencies = [
    "dnslib>=0.9",
    "rich>=13.0",
    "tomli>=2.0; python_version < '3.11'",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-n auto"
